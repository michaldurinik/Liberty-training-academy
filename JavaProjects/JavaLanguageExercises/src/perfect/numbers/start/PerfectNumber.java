package perfect.numbers.start;

/*
    Find all the factors of 'input' and
      see if their sum is its value
    Hints:
      1) You can use the modulus to find factors
      2) You can add the factors in pairs
      3) Don't add the same number twice (e.g. 4 * 4 = 16)
      4) You only need to work up to the square root
*/
public class PerfectNumber {
    public static boolean isPerfect(int input) {
        return false;
    }
}
